const { Router } = require('express')
const router = Router()
const { ReviewsBuy, BuyItems } = require('../db')

router.post('/', async (req, res) => {
  const { userId, publicationId } = req.body
  try {
    if
  } catch {

  }
})
